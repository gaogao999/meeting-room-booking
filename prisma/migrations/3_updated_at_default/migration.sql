BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[bookings] ADD CONSTRAINT [bookings_updated_at_df] DEFAULT CURRENT_TIMESTAMP FOR [updated_at];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

