BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[bookings] ADD [meeting_url] NVARCHAR(500),
[participants] NVARCHAR(max);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

